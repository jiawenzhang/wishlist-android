<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/menu_search"
        android:title="@string/search"
        android:icon="@android:drawable/ic_menu_search"
        app:showAsAction="ifRoom|collapseActionView"
        android:actionViewClass="android.widget.SearchView"
        />
    <!--android:icon="@android:drawable/ic_menu_search"-->
    <item android:id="@+id/menu_add"
        android:title="@string/add_new"
        android:icon="@android:drawable/ic_menu_camera"
        app:showAsAction="ifRoom"
        />
    <!--    <item android:id="@+id/menu_search"
          android:title="@string/search"
          android:icon="@android:drawable/ic_search_category_default"/>
            -->
    <!--   	<item android1:id="@+id/menu_post"
             android1:title="@string/post" android1:icon="@android:drawable/ic_menu_upload"/>
       <item android1:id="@+id/menu_scan"
             android1:title="@string/menu_scan" android1:icon="@android:drawable/ic_menu_view"/>
    -->
    <item android:id="@+id/menu_sort"
        android:title="Sort"
        />

    <item android:id="@+id/menu_status"
        android:title="Status"
        />

    <item android:id="@+id/menu_tags"
        android:title="Tags"
        />
</menu>
